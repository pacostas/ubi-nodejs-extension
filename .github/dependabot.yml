---
version: 2
updates:
- package-ecosystem: gomod
  directory: "/"
  schedule:
    interval: daily
  ignore:
    - dependency-name: "github.com/example/module"
  allow:
  # Allow both direct and indirect updates for all packages
  - dependency-type: "all"
